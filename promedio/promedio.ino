int Pin = 10;
int intensidad=64;
byte n=64;
int m=0;

void setup() {
  // put your setup code here, to run once:
  pinMode(Pin,OUTPUT);
}

void loop() {
  
  /*for (n;n<255;n++){}
    
        digitalWrite(Pin,HIGH);
        delayMicroseconds(10);
        //delayMicroseconds(intensidad*10);
        digitalWrite(Pin,LOW);
        delayMicroseconds(50);
        //delayMicroseconds(10*255-intensidad*10);
 */ 
}
